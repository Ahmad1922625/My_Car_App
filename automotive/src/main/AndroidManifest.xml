<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.MyCarApp">

    <!-- Define a custom permission to restrict who can send broadcasts -->
    <permission android:name="com.MyCarApp.PERMISSION_RECEIVE_MODULE"
        android:protectionLevel="signature"/>

    <uses-feature
        android:name="android.hardware.type.automotive"
        android:required="true" />

    <uses-permission android:name="android.car.permission.CONTROL_CAR_DOORS"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.car.permission.CONTROL_CAR_FEATURES"/>

    <application
        android:name=".MyCarApp"
        android:allowBackup="true"
        android:appCategory="audio"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyCarApp">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ Secure Broadcast Receiver -->
        <receiver android:name=".core.ModuleBroadcastReceiver"
            android:exported="true"
            android:permission="com.MyCarApp.PERMISSION_RECEIVE_MODULE">
            <intent-filter>
                <action android:name="com.MyCarApp.ACTIVATE_MODULE"/>
                <action android:name="com.MyCarApp.SET_DOOR_LOCK"/>
                <action android:name="com.MyCarApp.SET_DOOR_MOVE"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>
